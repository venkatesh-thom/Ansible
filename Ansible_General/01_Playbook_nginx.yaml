- name: nginx Setup
  hosts: local
  connection: local
  become: yes
  gather_facts: no
  tasks:
  - name: Installation
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start and enable nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
     
